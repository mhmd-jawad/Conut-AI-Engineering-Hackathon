APP_ENV=development
APP_HOST=127.0.0.1
APP_PORT=8000
DATA_DIR=./data/processed
MIN_SUPPORT=0.05
MIN_CONFIDENCE=0.2

# OpenAI (optional – enables LLM-powered intent classification)
# Without this key the agent falls back to regex-based classification.
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o
